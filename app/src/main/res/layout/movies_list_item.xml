<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_margin="10dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_movie_list_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:contentDescription="@string/movies_list_thumbnail_description"
        android:scaleType="centerCrop"
        android:layout_marginTop="@dimen/movies_list_thumbnail_margin_top"
        />

    <TextView
        android:id="@+id/tv_movie_list_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/movies_list_movie_title"
        android:layout_marginTop="@dimen/movies_list_movie_title_margin_top"
        android:fontFamily="monospace"
        />

</LinearLayout>